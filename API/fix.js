const mongoose = require('mongoose'); mongoose.connect('mongodb://127.0.0.1:27017/pawcare').then(async () => { const db = mongoose.connection.db; const orders = await db.collection('orders').find({}).toArray(); let count = 0; for (let order of orders) { if (order.statusHistory) { let changed = false; for (let sh of order.statusHistory) { if (typeof sh.changedBy === 'string') { try { sh.changedBy = new mongoose.Types.ObjectId(sh.changedBy); changed = true; } catch(e) {} } } if (changed) { await db.collection('orders').updateOne({_id: order._id}, {$set: {statusHistory: order.statusHistory}}); count++; } } } console.log('Fixed', count, 'orders'); process.exit(0); }).catch(console.error);
